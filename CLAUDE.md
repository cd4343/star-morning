# CLAUDE.md — Star Coin 项目开发规则

> 本规则适用于本项目中所有代码生成和修改任务，除非明确声明覆盖。核心偏好：非平凡工作上谨慎优先于速度。

---

## Rule 1 — 先思考，再编码

动手前先陈述假设。不确定就问，不猜。存在更简单方案时必须反驳。困惑时停下来。

**项目约束**：
- 已部署到生产服务器，任何修改必须在**不影响线上运行**的前提下进行
- SQLite 数据库不可替换，所有迁移必须幂等（`CREATE TABLE IF NOT EXISTS`、`ALTER TABLE ADD COLUMN` 加列前先检查）
- 后端 7200 行 `server.ts` 是已知技术债，新模块优先拆分到独立路由文件，但**不主动重构**未涉及的区域

---

## Rule 2 — 简洁优先

用最少的代码解决问题。不做推测性功能。不添加未被要求的能力。单次使用的代码不做抽象。

**项目特例**：
- 孩子端面向 6-12 岁儿童（ADHD 友好设计），UI 代码宁可多写几行条件判断，也不引入孩子无法理解的抽象交互
- 国际化（i18n）是已确认的长期目标，新模块中的**用户可见字符串**必须使用 `t('key')` 模式，即使当前只有中文

---

## Rule 3 — 外科手术式修改

只动必须动的。不"顺手优化"相邻代码。匹配现有风格，即便你不会这样写。不重构没坏的东西。每一行改动都能追溯到用户的需求。

**关键禁区**：
- 修改时间/审批/抽奖/惩罚/背包相关代码时，必须同时考虑家长端和孩子端的展示与接口
- 不修改 `临时/` 目录中的增量包——那是服务器同步的历史记录
- 不触碰 `stellar.db` 文件本身，只通过后端迁移逻辑操作
- 新增数据库字段只能追加（ADD COLUMN），不删除或重命名已有列
- API 路径保持 kebab-case，不改动已有路由的 URL

---

## Rule 4 — 目标驱动执行

定义成功标准，循环直到验证。将任务转化为可验证的目标（例如："为非法输入编写测试，然后让它们通过"）。

**项目验证标准**：
- 后端改动：启动无报错 + 相关 API curl 测试通过
- 前端改动：`npm run build` 零错误 + 页面可正常渲染
- 数据库迁移：后端重启后自动执行迁移，不丢失已有数据
- 每次提交前必须确认：改动范围与预期一致，未引入无关修改

---

## Rule 5 — 仅在需要判断时使用模型能力

适用：分类、起草、总结、提取。
不适用：路由、重试、状态码处理、确定性转换。
能用代码回答的，代码回答。

**项目应用**：
- 任务审核评分规则是确定性的数学公式，必须用纯代码实现，不依赖 AI
- 情绪场景推荐、探索地点建议等模糊场景才考虑模型辅助
- 金币/经验计算绝不允许浮点精度问题，使用整数运算

---

## Rule 6 — Token 预算不是建议

单任务：4,000 tokens。单会话：30,000 tokens。
接近预算时，总结并重新开始。暴露超支，不默默认 overrun。

**项目补充**：
- `server.ts` 7200 行，阅读时必须使用 offset/limit 分段读取，禁止全量加载
- 讨论后端改动时，精确定位行号范围，不粘贴大段代码

---

## Rule 7 — 暴露冲突，不取平均

两种模式冲突时，选一个（更新的 / 经过更多测试的），解释为什么，标记另一个待清理。不混合冲突模式。

**项目已知冲突**：
- 导航入口数量：需求池说 4 个，探索功能要求 5 个——以最新代码（5 入口）为准，需求池待更新
- `xp` vs `rewardXpTotal`：`xp` 是成长等级经验，`rewardXpTotal` 是特权进度账本——两者口径不同，不得混用
- `accumulatedRewardXp`（旧字段名）vs `rewardXpTotal`（当前字段名）——以 `rewardXpTotal` 为准

---

## Rule 8 — 先读后写

添加代码前，先阅读 exports、直接调用者、共享工具。不确定现有代码为何如此结构时，先问。

**项目必读清单**：
- 修改后端前：先读 `database.ts` 中相关表定义和 `server.ts` 中相关路由
- 修改前端前：先读 `api.ts` 中相关 API 调用和 `contexts/` 中相关状态
- 涉及奖励计算：先读 `rewardSystem.ts` 全部逻辑
- 涉及业务规则：先读 `.cursorrules` 中的长期记忆和 `docs/RULES_AND_ECONOMY.md`

---

## Rule 9 — 测试验证意图，不仅是行为

测试必须编码**为什么**行为重要，不仅是**什么**行为。业务逻辑变化时不能失败的测试是错误的。

**项目测试现状**：
- 后端零测试覆盖——新增模块必须至少覆盖关键路径
- 前端仅 Playwright smoke test——新页面必须可被 smoke test 覆盖
- 测试优先级：安全相关 > 金币/经验计算 > 审核流程 > UI 交互

---

## Rule 10 — 每个重要步骤后做检查点

总结做了什么、验证了什么、还剩什么。不从无法描述的状态继续。失去追踪时，停下来重新陈述。

**项目检查点模板**：
```
✅ 已完成：[具体改动]
✅ 已验证：[验证方式和结果]
⬜ 待完成：[剩余项]
⚠️ 风险点：[如有]
```

---

## Rule 11 — 匹配代码库的约定，即便你不认同

代码库内一致性 > 个人品味。认为约定有害时，提出而非默默另起炉灶。

**项目既有约定**：
- **时间**：所有业务时间统一使用北京时间 (UTC+8)，使用 `getBeijingDate`、`getLocalDateString`、`getBeijingTimeString`，不依赖 SQLite `localtime` 或服务器时区
- **命名**：TS 文件 camelCase；数据库表/列 snake_case；API 路径 kebab-case
- **禁止**：冗余注释、未使用导入、占位符代码、过度 console
- **必须**：错误处理、类型安全（逐步消除 `any`）、API 统一返回格式
- **认证**：JWT Token，角色 `parent` / `child`，Token 存 localStorage（已知安全风险，长期迁移至 httpOnly cookie）
- **数据库**：`sqlite` + `sqlite3` 驱动，`uuid` 生成主键，`bcryptjs` 密码哈希
- **前端**：React 18 + React Router + Tailwind CSS + lucide-react + axios
- **CSS**：Tailwind 优先，避免内联 style，组件用 CSS 变量保持一致

---

## Rule 12 — 失败要大声

"完成"在有任何静默跳过时是错误的。"测试通过"在任何测试被跳过时是错误的。默认暴露不确定性，不隐藏。

**项目关键失败场景**：
- 生产环境 JWT Secret 未设置 → 服务必须拒绝启动（`process.exit(1)`），不能只 warn
- 数据库迁移失败 → 必须明确报错，不能静默跳过
- API 返回非 200 → 必须处理错误状态，不能默默忽略
- 文件上传超出限制 → 必须拒绝并告知原因，不能静默丢弃

---

## 项目专属规则

### P1 — 生产安全第一

已部署到生产服务器，每次修改必须可独立回滚：
- 每个修改项独立提交，提交信息包含批次编号（如 `B1-1: JWT Secret 强制校验`）
- 附带回滚方式（恢复哪些文件/哪些行）
- 不替换服务器上的 `stellar.db`，让后端迁移自动补齐

### P2 — 移动端优先

最终目标为安卓和 iOS 端上线：
- 所有 UI 必须在 375px 宽度下可用
- 触控目标最小 44px × 44px
- 避免依赖 hover 状态
- iOS 安全区域适配（`env(safe-area-inset-*)`）
- 图片上传前压缩（最大 1MB），清除 EXIF 隐私信息
- 语音录制需验证 iOS Safari 的 MediaRecorder 兼容性

### P3 — ADHD 友好设计

面向注意力差异儿童，遵循以下原则：
- **短反馈优先**：3-10 分钟内看到完成结果
- **小步启动**：大任务必须拆成能开始的第一步
- **降低选择过载**：每次只突出 1 个主行动
- **高确定性低随机性**：宝箱抽奖有说明保底和可预期路径
- **情绪先行**：孩子急躁时先降强度
- **少惩罚多修复**：惩罚是修复机会，不是报复手段
- **审核后即时反馈**：不能让孩子"做了但不知道会得到什么"

### P4 — 国际化预留

初始定位中国地区儿童，架构需考虑全球扩展：
- 用户可见字符串使用 i18n key，不硬编码中文
- 探索功能 POI 搜索抽象为 `PoiSearchProvider` 接口，按环境变量选择高德/Google Places
- 日期/时间/货币格式通过 `Intl` API 处理，不硬编码格式
- 数据库文本字段使用 UTF-8，不假设字符长度

### P5 — 家庭探索模块特殊规则

探索功能是最新模块，有独立约束：
- **不是任务系统，不是金币系统**——不给金币，不给任务，记录体验和成长
- **不做实时定位**——不追踪位置，不获取 GPS 权限
- **家长发现地点，孩子自主打卡，家长确认补充**
- 媒体上传：base64 JSON → 保存到 `uploads/explore/`，随机文件名
  - MIME 白名单：`image/jpeg`、`image/png`、`image/webp`、`audio/webm`、`audio/mp4`
  - 单文件最大 5MB，家庭总配额 500MB
  - 文件名使用 `crypto.randomUUID()`，不保留原始文件名
- 新增 3 张表：`explore_places`、`explore_checkins`、`explore_media`
  - 必须包含索引：`(family_id, is_active)`、`(family_id, place_id)`、`(checkin_id)`
- 探索成就：5 种条件类型，通过成就事件机制集成，不硬编码检查逻辑

### P6 — 增量包管理

`临时/` 目录用于服务器更新同步：
- 命名格式：`starcoin-incremental-patch-YYYYMMDD-HHMMSS-描述-code-only/`
- 每个包含 `REPLACE_FILES.md` 描述替换路径
- **不修改、不删除**已有增量包
- 超过 30 天的增量包可归档到 `_archive/`
- 当前临时目录 203MB，部署后应及时清理

---

## 快速参考

| 项目 | 值 |
|------|-----|
| 项目名 | 星辰早晨 / Star Coin |
| 版本 | v1.1.3 + 家庭探索增量包 |
| 前端 | React 18 + TypeScript + Vite + Tailwind CSS |
| 后端 | Node.js + Express + TypeScript |
| 数据库 | SQLite（`stellar.db`） |
| 认证 | JWT（`parent` / `child` 角色） |
| 时区 | 北京时间 UTC+8 |
| 部署 | Python HTTP 服务器(80) + Node.js 后端(3001) |
| 目标平台 | Web → Android + iOS（Capacitor） |
| 核心文档 | `.cursorrules`、`docs/RULES_AND_ECONOMY.md`、`docs/ARCHITECTURE.md` |
| 增量修复方案 | `docs/superpowers/specs/2026-06-09-incremental-fix-plan.md` |
